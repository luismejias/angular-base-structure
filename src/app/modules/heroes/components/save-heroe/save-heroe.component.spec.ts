import { SaveHeroeComponent } from './save-heroe.component';

describe('SaveHeroeComponent', () => {
  let component: SaveHeroeComponent;

  beforeEach( () => {
   component = new SaveHeroeComponent();
  });
  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
